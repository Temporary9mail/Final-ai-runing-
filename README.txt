Deploy this folder as a Render Web Service.
Build: npm install
Start: npm start
Required Render Environment Variable: OPENAI_API_KEY
Optional: OPENAI_MODEL=gpt-5.6-luna
Optional: ALLOWED_ORIGIN=https://YOUR-USERNAME.github.io
After deploy, copy the Render URL into frontend api-config.js.
